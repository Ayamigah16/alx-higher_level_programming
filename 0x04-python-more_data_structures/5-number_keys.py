#!/usr/bin/python3

def number_keys(a_dictionary):
    # Get the keys of the dictionary using the keys() method and calculate the length
    num_keys = len(a_dictionary.keys())

    # Return the number of keys
    return num_keys
