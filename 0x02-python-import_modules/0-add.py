#!/usr/bin/python3
from add_0 import add

if __name__ == "__main__":
    # code will run only when 0-add.py is run
    a = 1
    b = 2
    result = add(a, b)
    print("{} + {} = {}".format(a, b, result))
